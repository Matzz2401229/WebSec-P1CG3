upstream dashboard_backend {
  server dashboard:3000;
  keepalive 64;
}

server {
    listen 80;
    server_name localhost;

    # Frontend UI + Static
    location /frontend/ {
        auth_basic "WAFGuard Frontend";
        auth_basic_user_file /etc/nginx/.htpasswd;

        location ~ ^/frontend/static {
        proxy_pass http://frontend:3000;
        } 
        proxy_pass http://frontend:3000/;
        proxy_set_header Host $host;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection upgrade;
}

    # Dashboard UI
    location /dashboard/ {
        auth_basic "WAFGuard Admin";
        auth_basic_user_file /etc/nginx/.htpasswd;
        rewrite ^/dashboard/(.*)$ /$1 break;
        proxy_pass http://dashboard:3000;
    }

    # CRITICAL: Catch frontend API calls (localhost:3001 → dashboard)
    location ~ ^/(api|events|logs|metrics|stats)/ {
        proxy_pass http://dashboard:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

      # API proxy (frontend calls)
    location /api/ {
        proxy_pass http://dashboard_backend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        proxy_buffering off;
    }

    # CORS for all
    location / {
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS';
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';
            return 204;
        }
        add_header 'Access-Control-Allow-Origin' '*' always;
<<<<<<< HEAD

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
=======
>>>>>>> origin/Matin
        proxy_pass http://modsecurity:8080/;
    }
}
